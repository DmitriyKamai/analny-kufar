meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  # Long-running Telegram polling bot (no HTTP server).
  scriptName: notifier.py
  # Persist config + cache across rebuilds.
  persistenceMount: /data
